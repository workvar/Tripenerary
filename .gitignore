node_modules/
.expo/
dist/
*.log
.DS_Store

# Secrets
.env
.env.*
!.env.example
*.keystore
!android/app/debug.keystore
keystore.properties

# iOS stays generated by `expo prebuild`.
ios/

# android/ is intentionally NOT ignored. The release build depends on native
# config (Maps key injection, signing, splash, trimmed permissions) that would
# be lost on every prebuild. Build artifacts inside it are ignored by
# android/.gitignore.
