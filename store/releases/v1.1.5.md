## v1.1.5

Upload-key signed Play AAB (`CN=Tripenerary`). Fixes the CI signing verification step that aborted after a successful release build.

### Highlights

- **Play-ready signing.** Bundle signed with the upload keystore, not Android Debug.
- **Verify step fixed.** jarsigner checks no longer fail on pipefail/`head`.

### Downloads

| Platform | Asset |
| --- | --- |
| Android App Bundle (Play) | `tripenerary-1.1.5.aab` |
| Android APK | `tripenerary-1.1.5.apk` |

### Upgrade notes

- Upload `tripenerary-1.1.5.aab` to Play Console (replace any debug-signed `v1.1.2` attempt).
- Use first-release what’s-new from `store/whatsnew/whatsnew-en-US`.

**Full Changelog**: https://github.com/workvar/Tripenerary/compare/v1.1.4...v1.1.5
